[tools]
go = "1.26.8"
golangci-lint = "latest"
"go:golang.org/x/vuln/cmd/govulncheck" = "latest"
"go:github.com/a-h/templ/cmd/templ" = "v0.3.1020"
lefthook = "latest"
"github:Dicklesworthstone/destructive_command_guard" = "latest"

[tasks.fmt]
run = "templ generate && gofmt -w $(find . -name '*.go' -not -path './.git/*')"

[tasks.lint]
run = "golangci-lint run ./..."

[tasks.test]
run = "go test -cover ./..."

[tasks.ci]
run = "mise run fmt && mise run lint && mise run test && govulncheck ./..."
